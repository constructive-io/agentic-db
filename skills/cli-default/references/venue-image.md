# venueImage

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for VenueImage records via agent-db CLI

## Usage

```bash
agent-db venue-image list
agent-db venue-image get --id <value>
agent-db venue-image create --venueId <value> --imageId <value> --entityId <value>
agent-db venue-image update --id <value> [--venueId <value>] [--imageId <value>] [--entityId <value>]
agent-db venue-image delete --id <value>
```

## Examples

### List all venueImage records

```bash
agent-db venue-image list
```

### Create a venueImage

```bash
agent-db venue-image create --venueId <value> --imageId <value> --entityId <value>
```

### Get a venueImage by id

```bash
agent-db venue-image get --id <value>
```
