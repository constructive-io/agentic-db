# contactCompany

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for ContactCompany records via agent-db CLI

## Usage

```bash
agent-db contact-company list
agent-db contact-company get --id <value>
agent-db contact-company create --contactId <value> --companyId <value> --entityId <value>
agent-db contact-company update --id <value> [--contactId <value>] [--companyId <value>] [--entityId <value>]
agent-db contact-company delete --id <value>
```

## Examples

### List all contactCompany records

```bash
agent-db contact-company list
```

### Create a contactCompany

```bash
agent-db contact-company create --contactId <value> --companyId <value> --entityId <value>
```

### Get a contactCompany by id

```bash
agent-db contact-company get --id <value>
```
