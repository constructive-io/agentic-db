# phoneNumbersModule

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for PhoneNumbersModule records via agentic-db CLI

## Usage

```bash
agentic-db phone-numbers-module list
agentic-db phone-numbers-module get --id <UUID>
agentic-db phone-numbers-module create --databaseId <UUID> --tableName <String> [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>]
agentic-db phone-numbers-module update --id <UUID> [--databaseId <UUID>] [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>] [--tableName <String>]
agentic-db phone-numbers-module delete --id <UUID>
```

## Examples

### List all phoneNumbersModule records

```bash
agentic-db phone-numbers-module list
```

### Create a phoneNumbersModule

```bash
agentic-db phone-numbers-module create --databaseId <UUID> --tableName <String> [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>]
```

### Get a phoneNumbersModule by id

```bash
agentic-db phone-numbers-module get --id <value>
```
