# orgGetSubordinatesRecord

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for OrgGetSubordinatesRecord records via agentic-db CLI

## Usage

```bash
agentic-db org-get-subordinates-record list
agentic-db org-get-subordinates-record get --id <UUID>
agentic-db org-get-subordinates-record create --userId <UUID> --depth <Int>
agentic-db org-get-subordinates-record update --id <UUID> [--userId <UUID>] [--depth <Int>]
agentic-db org-get-subordinates-record delete --id <UUID>
```

## Examples

### List all orgGetSubordinatesRecord records

```bash
agentic-db org-get-subordinates-record list
```

### Create a orgGetSubordinatesRecord

```bash
agentic-db org-get-subordinates-record create --userId <UUID> --depth <Int>
```

### Get a orgGetSubordinatesRecord by id

```bash
agentic-db org-get-subordinates-record get --id <value>
```
