# defaultIdsModule

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for DefaultIdsModule records via agentic-db CLI

## Usage

```bash
agentic-db default-ids-module list
agentic-db default-ids-module get --id <UUID>
agentic-db default-ids-module create --databaseId <UUID>
agentic-db default-ids-module update --id <UUID> [--databaseId <UUID>]
agentic-db default-ids-module delete --id <UUID>
```

## Examples

### List all defaultIdsModule records

```bash
agentic-db default-ids-module list
```

### Create a defaultIdsModule

```bash
agentic-db default-ids-module create --databaseId <UUID>
```

### Get a defaultIdsModule by id

```bash
agentic-db default-ids-module get --id <value>
```
