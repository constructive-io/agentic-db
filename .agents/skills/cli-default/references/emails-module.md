# emailsModule

<!-- @constructive-io/graphql-codegen - DO NOT EDIT -->

CRUD operations for EmailsModule records via agentic-db CLI

## Usage

```bash
agentic-db emails-module list
agentic-db emails-module get --id <UUID>
agentic-db emails-module create --databaseId <UUID> --tableName <String> [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>]
agentic-db emails-module update --id <UUID> [--databaseId <UUID>] [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>] [--tableName <String>]
agentic-db emails-module delete --id <UUID>
```

## Examples

### List all emailsModule records

```bash
agentic-db emails-module list
```

### Create a emailsModule

```bash
agentic-db emails-module create --databaseId <UUID> --tableName <String> [--schemaId <UUID>] [--privateSchemaId <UUID>] [--tableId <UUID>] [--ownerTableId <UUID>]
```

### Get a emailsModule by id

```bash
agentic-db emails-module get --id <value>
```
