apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default overlay: bundled Ollama running on CPU inside the cluster.
# Use this for development or when no GPU is available.

resources:
  - ../../base
  - ../../base/ollama.yaml
