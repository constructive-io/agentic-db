# orm-orgProfile

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT

ORM operations for OrgProfile records

## Usage

```typescript
db.orgProfile.findMany({ select: { id: true } }).execute()
db.orgProfile.findOne({ where: { id: '<value>' }, select: { id: true } }).execute()
db.orgProfile.create({ data: { name: '<value>', slug: '<value>', description: '<value>', permissions: '<value>', isSystem: '<value>', isDefault: '<value>', entityId: '<value>' }, select: { id: true } }).execute()
db.orgProfile.update({ where: { id: '<value>' }, data: { name: '<new>' }, select: { id: true } }).execute()
db.orgProfile.delete({ where: { id: '<value>' } }).execute()
```

## Examples

### List all orgProfile records

```typescript
const items = await db.orgProfile.findMany({
  select: { id: true, name: true }
}).execute();
```

### Create a orgProfile

```typescript
const item = await db.orgProfile.create({
  data: { name: 'value', slug: 'value', description: 'value', permissions: 'value', isSystem: 'value', isDefault: 'value', entityId: 'value' },
  select: { id: true }
}).execute();
```
