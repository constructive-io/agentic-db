# orm-appProfilePermission

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT

ORM operations for AppProfilePermission records

## Usage

```typescript
db.appProfilePermission.findMany({ select: { id: true } }).execute()
db.appProfilePermission.findOne({ where: { id: '<value>' }, select: { id: true } }).execute()
db.appProfilePermission.create({ data: { profileId: '<value>', permissionId: '<value>' }, select: { id: true } }).execute()
db.appProfilePermission.update({ where: { id: '<value>' }, data: { profileId: '<new>' }, select: { id: true } }).execute()
db.appProfilePermission.delete({ where: { id: '<value>' } }).execute()
```

## Examples

### List all appProfilePermission records

```typescript
const items = await db.appProfilePermission.findMany({
  select: { id: true, profileId: true }
}).execute();
```

### Create a appProfilePermission

```typescript
const item = await db.appProfilePermission.create({
  data: { profileId: 'value', permissionId: 'value' },
  select: { id: true }
}).execute();
```
