# orm-appProfileDefinitionGrant

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT

ORM operations for AppProfileDefinitionGrant records

## Usage

```typescript
db.appProfileDefinitionGrant.findMany({ select: { id: true } }).execute()
db.appProfileDefinitionGrant.findOne({ where: { id: '<value>' }, select: { id: true } }).execute()
db.appProfileDefinitionGrant.create({ data: { profileId: '<value>', permissionId: '<value>', grantorId: '<value>', isGrant: '<value>' }, select: { id: true } }).execute()
db.appProfileDefinitionGrant.update({ where: { id: '<value>' }, data: { profileId: '<new>' }, select: { id: true } }).execute()
db.appProfileDefinitionGrant.delete({ where: { id: '<value>' } }).execute()
```

## Examples

### List all appProfileDefinitionGrant records

```typescript
const items = await db.appProfileDefinitionGrant.findMany({
  select: { id: true, profileId: true }
}).execute();
```

### Create a appProfileDefinitionGrant

```typescript
const item = await db.appProfileDefinitionGrant.create({
  data: { profileId: 'value', permissionId: 'value', grantorId: 'value', isGrant: 'value' },
  select: { id: true }
}).execute();
```
