# orm-appProfile

> @generated by @constructive-io/graphql-codegen - DO NOT EDIT

ORM operations for AppProfile records

## Usage

```typescript
db.appProfile.findMany({ select: { id: true } }).execute()
db.appProfile.findOne({ where: { id: '<value>' }, select: { id: true } }).execute()
db.appProfile.create({ data: { name: '<value>', slug: '<value>', description: '<value>', permissions: '<value>', isSystem: '<value>', isDefault: '<value>' }, select: { id: true } }).execute()
db.appProfile.update({ where: { id: '<value>' }, data: { name: '<new>' }, select: { id: true } }).execute()
db.appProfile.delete({ where: { id: '<value>' } }).execute()
```

## Examples

### List all appProfile records

```typescript
const items = await db.appProfile.findMany({
  select: { id: true, name: true }
}).execute();
```

### Create a appProfile

```typescript
const item = await db.appProfile.create({
  data: { name: 'value', slug: 'value', description: 'value', permissions: 'value', isSystem: 'value', isDefault: 'value' },
  select: { id: true }
}).execute();
```
