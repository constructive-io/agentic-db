/**
 * CLI commands for ListItem
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateListItemInput, ListItemPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  listId: 'uuid',
  content: 'string',
  position: 'int',
  isChecked: 'boolean',
  refId: 'uuid',
  refType: 'string',
  contentTrgmSimilarity: 'float',
  refTypeTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\nlist-item <command>\n\nCommands:\n  list                  List all listItem records\n  get                   Get a listItem by ID\n  create                Create a new listItem\n  update                Update an existing listItem\n  delete                Delete a listItem\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.listItem
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          listId: true,
          content: true,
          position: true,
          isChecked: true,
          refId: true,
          refType: true,
          contentTrgmSimilarity: true,
          refTypeTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.listItem
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          listId: true,
          content: true,
          position: true,
          isChecked: true,
          refId: true,
          refType: true,
          contentTrgmSimilarity: true,
          refTypeTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'listId',
        message: 'listId',
        required: true,
      },
      {
        type: 'text',
        name: 'content',
        message: 'content',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'position',
        message: 'position',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isChecked',
        message: 'isChecked',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'refId',
        message: 'refId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'refType',
        message: 'refType',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateListItemInput['listItem'];
    const client = getClient();
    const result = await client.listItem
      .create({
        data: {
          entityId: cleanedData.entityId,
          listId: cleanedData.listId,
          content: cleanedData.content,
          position: cleanedData.position,
          isChecked: cleanedData.isChecked,
          refId: cleanedData.refId,
          refType: cleanedData.refType,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          listId: true,
          content: true,
          position: true,
          isChecked: true,
          refId: true,
          refType: true,
          contentTrgmSimilarity: true,
          refTypeTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'listId',
        message: 'listId',
        required: false,
      },
      {
        type: 'text',
        name: 'content',
        message: 'content',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'position',
        message: 'position',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isChecked',
        message: 'isChecked',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'refId',
        message: 'refId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'refType',
        message: 'refType',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ListItemPatch;
    const client = getClient();
    const result = await client.listItem
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          listId: cleanedData.listId,
          content: cleanedData.content,
          position: cleanedData.position,
          isChecked: cleanedData.isChecked,
          refId: cleanedData.refId,
          refType: cleanedData.refType,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          listId: true,
          content: true,
          position: true,
          isChecked: true,
          refId: true,
          refType: true,
          contentTrgmSimilarity: true,
          refTypeTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.listItem
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
