/**
 * CLI commands for SkillExecution
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateSkillExecutionInput, SkillExecutionPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  skillId: 'uuid',
  agentId: 'uuid',
  sessionId: 'uuid',
  status: 'string',
  startedAt: 'string',
  completedAt: 'string',
  durationMs: 'int',
  input: 'json',
  output: 'json',
  error: 'string',
  statusTrgmSimilarity: 'float',
  errorTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\nskill-execution <command>\n\nCommands:\n  list                  List all skillExecution records\n  get                   Get a skillExecution by ID\n  create                Create a new skillExecution\n  update                Update an existing skillExecution\n  delete                Delete a skillExecution\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.skillExecution
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          skillId: true,
          agentId: true,
          sessionId: true,
          status: true,
          startedAt: true,
          completedAt: true,
          durationMs: true,
          input: true,
          output: true,
          error: true,
          statusTrgmSimilarity: true,
          errorTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.skillExecution
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          skillId: true,
          agentId: true,
          sessionId: true,
          status: true,
          startedAt: true,
          completedAt: true,
          durationMs: true,
          input: true,
          output: true,
          error: true,
          statusTrgmSimilarity: true,
          errorTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'skillId',
        message: 'skillId',
        required: true,
      },
      {
        type: 'text',
        name: 'agentId',
        message: 'agentId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'input',
        message: 'input',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'output',
        message: 'output',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateSkillExecutionInput['skillExecution'];
    const client = getClient();
    const result = await client.skillExecution
      .create({
        data: {
          entityId: cleanedData.entityId,
          skillId: cleanedData.skillId,
          agentId: cleanedData.agentId,
          sessionId: cleanedData.sessionId,
          status: cleanedData.status,
          startedAt: cleanedData.startedAt,
          completedAt: cleanedData.completedAt,
          durationMs: cleanedData.durationMs,
          input: cleanedData.input,
          output: cleanedData.output,
          error: cleanedData.error,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          skillId: true,
          agentId: true,
          sessionId: true,
          status: true,
          startedAt: true,
          completedAt: true,
          durationMs: true,
          input: true,
          output: true,
          error: true,
          statusTrgmSimilarity: true,
          errorTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'skillId',
        message: 'skillId',
        required: false,
      },
      {
        type: 'text',
        name: 'agentId',
        message: 'agentId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'status',
        message: 'status',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startedAt',
        message: 'startedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'durationMs',
        message: 'durationMs',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'input',
        message: 'input',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'output',
        message: 'output',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'error',
        message: 'error',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as SkillExecutionPatch;
    const client = getClient();
    const result = await client.skillExecution
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          skillId: cleanedData.skillId,
          agentId: cleanedData.agentId,
          sessionId: cleanedData.sessionId,
          status: cleanedData.status,
          startedAt: cleanedData.startedAt,
          completedAt: cleanedData.completedAt,
          durationMs: cleanedData.durationMs,
          input: cleanedData.input,
          output: cleanedData.output,
          error: cleanedData.error,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          skillId: true,
          agentId: true,
          sessionId: true,
          status: true,
          startedAt: true,
          completedAt: true,
          durationMs: true,
          input: true,
          output: true,
          error: true,
          statusTrgmSimilarity: true,
          errorTrgmSimilarity: true,
          searchScore: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.skillExecution
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
