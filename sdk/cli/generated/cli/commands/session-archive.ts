/**
 * CLI commands for SessionArchive
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateSessionArchiveInput, SessionArchivePatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  sessionId: 'uuid',
  archiveIndex: 'int',
  summary: 'string',
  messageRangeStart: 'int',
  messageRangeEnd: 'int',
  rawMessages: 'json',
  embeddingText: 'string',
  embedding: 'string',
  embeddingTextBm25Score: 'float',
  embeddingVectorDistance: 'float',
  summaryTrgmSimilarity: 'float',
  embeddingTextTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\nsession-archive <command>\n\nCommands:\n  list                  List all sessionArchive records\n  get                   Get a sessionArchive by ID\n  create                Create a new sessionArchive\n  update                Update an existing sessionArchive\n  delete                Delete a sessionArchive\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.sessionArchive
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          sessionId: true,
          archiveIndex: true,
          summary: true,
          messageRangeStart: true,
          messageRangeEnd: true,
          rawMessages: true,
          embeddingText: true,
          embedding: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.sessionArchive
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          sessionId: true,
          archiveIndex: true,
          summary: true,
          messageRangeStart: true,
          messageRangeEnd: true,
          rawMessages: true,
          embeddingText: true,
          embedding: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: true,
      },
      {
        type: 'text',
        name: 'archiveIndex',
        message: 'archiveIndex',
        required: true,
      },
      {
        type: 'text',
        name: 'summary',
        message: 'summary',
        required: true,
      },
      {
        type: 'text',
        name: 'messageRangeStart',
        message: 'messageRangeStart',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'messageRangeEnd',
        message: 'messageRangeEnd',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'rawMessages',
        message: 'rawMessages',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateSessionArchiveInput['sessionArchive'];
    const client = getClient();
    const result = await client.sessionArchive
      .create({
        data: {
          entityId: cleanedData.entityId,
          sessionId: cleanedData.sessionId,
          archiveIndex: cleanedData.archiveIndex,
          summary: cleanedData.summary,
          messageRangeStart: cleanedData.messageRangeStart,
          messageRangeEnd: cleanedData.messageRangeEnd,
          rawMessages: cleanedData.rawMessages,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          sessionId: true,
          archiveIndex: true,
          summary: true,
          messageRangeStart: true,
          messageRangeEnd: true,
          rawMessages: true,
          embeddingText: true,
          embedding: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'sessionId',
        message: 'sessionId',
        required: false,
      },
      {
        type: 'text',
        name: 'archiveIndex',
        message: 'archiveIndex',
        required: false,
      },
      {
        type: 'text',
        name: 'summary',
        message: 'summary',
        required: false,
      },
      {
        type: 'text',
        name: 'messageRangeStart',
        message: 'messageRangeStart',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'messageRangeEnd',
        message: 'messageRangeEnd',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'rawMessages',
        message: 'rawMessages',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as SessionArchivePatch;
    const client = getClient();
    const result = await client.sessionArchive
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          sessionId: cleanedData.sessionId,
          archiveIndex: cleanedData.archiveIndex,
          summary: cleanedData.summary,
          messageRangeStart: cleanedData.messageRangeStart,
          messageRangeEnd: cleanedData.messageRangeEnd,
          rawMessages: cleanedData.rawMessages,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          sessionId: true,
          archiveIndex: true,
          summary: true,
          messageRangeStart: true,
          messageRangeEnd: true,
          rawMessages: true,
          embeddingText: true,
          embedding: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.sessionArchive
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
