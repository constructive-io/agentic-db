/**
 * CLI commands for ActivityLog
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateActivityLogInput, ActivityLogPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  activityType: 'string',
  completedAt: 'string',
  durationMinutes: 'int',
  quantity: 'string',
  quantityUnit: 'string',
  intensity: 'string',
  notes: 'string',
  meta: 'json',
  tags: 'string',
  createdAt: 'string',
  updatedAt: 'string',
  embeddingText: 'string',
  embedding: 'string',
  embeddingStale: 'boolean',
  habitId: 'uuid',
  embeddingTextBm25Score: 'float',
  embeddingVectorDistance: 'float',
  activityTypeTrgmSimilarity: 'float',
  quantityUnitTrgmSimilarity: 'float',
  intensityTrgmSimilarity: 'float',
  notesTrgmSimilarity: 'float',
  embeddingTextTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\nactivity-log <command>\n\nCommands:\n  list                  List all activityLog records\n  get                   Get a activityLog by ID\n  create                Create a new activityLog\n  update                Update an existing activityLog\n  delete                Delete a activityLog\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.activityLog
      .findMany({
        select: {
          id: true,
          entityId: true,
          activityType: true,
          completedAt: true,
          durationMinutes: true,
          quantity: true,
          quantityUnit: true,
          intensity: true,
          notes: true,
          meta: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          habitId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.activityLog
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          activityType: true,
          completedAt: true,
          durationMinutes: true,
          quantity: true,
          quantityUnit: true,
          intensity: true,
          notes: true,
          meta: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          habitId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'activityType',
        message: 'activityType',
        required: true,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: true,
      },
      {
        type: 'text',
        name: 'durationMinutes',
        message: 'durationMinutes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'quantity',
        message: 'quantity',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'quantityUnit',
        message: 'quantityUnit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'intensity',
        message: 'intensity',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'notes',
        message: 'notes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'meta',
        message: 'meta',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'embeddingStale',
        message: 'embeddingStale',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'habitId',
        message: 'habitId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateActivityLogInput['activityLog'];
    const client = getClient();
    const result = await client.activityLog
      .create({
        data: {
          entityId: cleanedData.entityId,
          activityType: cleanedData.activityType,
          completedAt: cleanedData.completedAt,
          durationMinutes: cleanedData.durationMinutes,
          quantity: cleanedData.quantity,
          quantityUnit: cleanedData.quantityUnit,
          intensity: cleanedData.intensity,
          notes: cleanedData.notes,
          meta: cleanedData.meta,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
          embeddingStale: cleanedData.embeddingStale,
          habitId: cleanedData.habitId,
        },
        select: {
          id: true,
          entityId: true,
          activityType: true,
          completedAt: true,
          durationMinutes: true,
          quantity: true,
          quantityUnit: true,
          intensity: true,
          notes: true,
          meta: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          habitId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'activityType',
        message: 'activityType',
        required: false,
      },
      {
        type: 'text',
        name: 'completedAt',
        message: 'completedAt',
        required: false,
      },
      {
        type: 'text',
        name: 'durationMinutes',
        message: 'durationMinutes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'quantity',
        message: 'quantity',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'quantityUnit',
        message: 'quantityUnit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'intensity',
        message: 'intensity',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'notes',
        message: 'notes',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'meta',
        message: 'meta',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'embeddingStale',
        message: 'embeddingStale',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'habitId',
        message: 'habitId',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ActivityLogPatch;
    const client = getClient();
    const result = await client.activityLog
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          activityType: cleanedData.activityType,
          completedAt: cleanedData.completedAt,
          durationMinutes: cleanedData.durationMinutes,
          quantity: cleanedData.quantity,
          quantityUnit: cleanedData.quantityUnit,
          intensity: cleanedData.intensity,
          notes: cleanedData.notes,
          meta: cleanedData.meta,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
          embeddingStale: cleanedData.embeddingStale,
          habitId: cleanedData.habitId,
        },
        select: {
          id: true,
          entityId: true,
          activityType: true,
          completedAt: true,
          durationMinutes: true,
          quantity: true,
          quantityUnit: true,
          intensity: true,
          notes: true,
          meta: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          habitId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.activityLog
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
