/**
 * CLI commands for WorkflowStep
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateWorkflowStepInput, WorkflowStepPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  workflowId: 'uuid',
  stepOrder: 'int',
  actionType: 'string',
  actionConfig: 'json',
  onSuccessStep: 'int',
  onFailureStep: 'int',
  timeoutMs: 'int',
};
const usage =
  '\nworkflow-step <command>\n\nCommands:\n  list                  List all workflowStep records\n  get                   Get a workflowStep by ID\n  create                Create a new workflowStep\n  update                Update an existing workflowStep\n  delete                Delete a workflowStep\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.workflowStep
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          workflowId: true,
          stepOrder: true,
          actionType: true,
          actionConfig: true,
          onSuccessStep: true,
          onFailureStep: true,
          timeoutMs: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.workflowStep
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          workflowId: true,
          stepOrder: true,
          actionType: true,
          actionConfig: true,
          onSuccessStep: true,
          onFailureStep: true,
          timeoutMs: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'workflowId',
        message: 'workflowId',
        required: true,
      },
      {
        type: 'text',
        name: 'stepOrder',
        message: 'stepOrder',
        required: true,
      },
      {
        type: 'text',
        name: 'actionType',
        message: 'actionType',
        required: true,
      },
      {
        type: 'json',
        name: 'actionConfig',
        message: 'actionConfig',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'onSuccessStep',
        message: 'onSuccessStep',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'onFailureStep',
        message: 'onFailureStep',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'timeoutMs',
        message: 'timeoutMs',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateWorkflowStepInput['workflowStep'];
    const client = getClient();
    const result = await client.workflowStep
      .create({
        data: {
          entityId: cleanedData.entityId,
          workflowId: cleanedData.workflowId,
          stepOrder: cleanedData.stepOrder,
          actionType: cleanedData.actionType,
          actionConfig: cleanedData.actionConfig,
          onSuccessStep: cleanedData.onSuccessStep,
          onFailureStep: cleanedData.onFailureStep,
          timeoutMs: cleanedData.timeoutMs,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          workflowId: true,
          stepOrder: true,
          actionType: true,
          actionConfig: true,
          onSuccessStep: true,
          onFailureStep: true,
          timeoutMs: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'workflowId',
        message: 'workflowId',
        required: false,
      },
      {
        type: 'text',
        name: 'stepOrder',
        message: 'stepOrder',
        required: false,
      },
      {
        type: 'text',
        name: 'actionType',
        message: 'actionType',
        required: false,
      },
      {
        type: 'json',
        name: 'actionConfig',
        message: 'actionConfig',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'onSuccessStep',
        message: 'onSuccessStep',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'onFailureStep',
        message: 'onFailureStep',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'timeoutMs',
        message: 'timeoutMs',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as WorkflowStepPatch;
    const client = getClient();
    const result = await client.workflowStep
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          workflowId: cleanedData.workflowId,
          stepOrder: cleanedData.stepOrder,
          actionType: cleanedData.actionType,
          actionConfig: cleanedData.actionConfig,
          onSuccessStep: cleanedData.onSuccessStep,
          onFailureStep: cleanedData.onFailureStep,
          timeoutMs: cleanedData.timeoutMs,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          workflowId: true,
          stepOrder: true,
          actionType: true,
          actionConfig: true,
          onSuccessStep: true,
          onFailureStep: true,
          timeoutMs: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.workflowStep
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
