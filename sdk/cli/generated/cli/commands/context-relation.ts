/**
 * CLI commands for ContextRelation
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateContextRelationInput, ContextRelationPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  fromType: 'string',
  fromId: 'uuid',
  toType: 'string',
  toId: 'uuid',
  relationKind: 'string',
  reason: 'string',
  strength: 'string',
  fromTypeTrgmSimilarity: 'float',
  toTypeTrgmSimilarity: 'float',
  relationKindTrgmSimilarity: 'float',
  reasonTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\ncontext-relation <command>\n\nCommands:\n  list                  List all contextRelation records\n  get                   Get a contextRelation by ID\n  create                Create a new contextRelation\n  update                Update an existing contextRelation\n  delete                Delete a contextRelation\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.contextRelation
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          fromType: true,
          fromId: true,
          toType: true,
          toId: true,
          relationKind: true,
          reason: true,
          strength: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.contextRelation
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          fromType: true,
          fromId: true,
          toType: true,
          toId: true,
          relationKind: true,
          reason: true,
          strength: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'fromType',
        message: 'fromType',
        required: true,
      },
      {
        type: 'text',
        name: 'fromId',
        message: 'fromId',
        required: true,
      },
      {
        type: 'text',
        name: 'toType',
        message: 'toType',
        required: true,
      },
      {
        type: 'text',
        name: 'toId',
        message: 'toId',
        required: true,
      },
      {
        type: 'text',
        name: 'relationKind',
        message: 'relationKind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'reason',
        message: 'reason',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'strength',
        message: 'strength',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateContextRelationInput['contextRelation'];
    const client = getClient();
    const result = await client.contextRelation
      .create({
        data: {
          entityId: cleanedData.entityId,
          fromType: cleanedData.fromType,
          fromId: cleanedData.fromId,
          toType: cleanedData.toType,
          toId: cleanedData.toId,
          relationKind: cleanedData.relationKind,
          reason: cleanedData.reason,
          strength: cleanedData.strength,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          fromType: true,
          fromId: true,
          toType: true,
          toId: true,
          relationKind: true,
          reason: true,
          strength: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'fromType',
        message: 'fromType',
        required: false,
      },
      {
        type: 'text',
        name: 'fromId',
        message: 'fromId',
        required: false,
      },
      {
        type: 'text',
        name: 'toType',
        message: 'toType',
        required: false,
      },
      {
        type: 'text',
        name: 'toId',
        message: 'toId',
        required: false,
      },
      {
        type: 'text',
        name: 'relationKind',
        message: 'relationKind',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'reason',
        message: 'reason',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'strength',
        message: 'strength',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ContextRelationPatch;
    const client = getClient();
    const result = await client.contextRelation
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          fromType: cleanedData.fromType,
          fromId: cleanedData.fromId,
          toType: cleanedData.toType,
          toId: cleanedData.toId,
          relationKind: cleanedData.relationKind,
          reason: cleanedData.reason,
          strength: cleanedData.strength,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          fromType: true,
          fromId: true,
          toType: true,
          toId: true,
          relationKind: true,
          reason: true,
          strength: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.contextRelation
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
