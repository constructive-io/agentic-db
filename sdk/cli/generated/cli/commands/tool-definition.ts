/**
 * CLI commands for ToolDefinition
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateToolDefinitionInput, ToolDefinitionPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  name: 'string',
  description: 'string',
  inputSchema: 'json',
  outputSchema: 'json',
  implementation: 'string',
  isActive: 'boolean',
  createdAt: 'string',
  updatedAt: 'string',
};
const usage =
  '\ntool-definition <command>\n\nCommands:\n  list                  List all toolDefinition records\n  get                   Get a toolDefinition by ID\n  create                Create a new toolDefinition\n  update                Update an existing toolDefinition\n  delete                Delete a toolDefinition\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.toolDefinition
      .findMany({
        select: {
          id: true,
          entityId: true,
          name: true,
          description: true,
          inputSchema: true,
          outputSchema: true,
          implementation: true,
          isActive: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.toolDefinition
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          name: true,
          description: true,
          inputSchema: true,
          outputSchema: true,
          implementation: true,
          isActive: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'inputSchema',
        message: 'inputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'outputSchema',
        message: 'outputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'implementation',
        message: 'implementation',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateToolDefinitionInput['toolDefinition'];
    const client = getClient();
    const result = await client.toolDefinition
      .create({
        data: {
          entityId: cleanedData.entityId,
          name: cleanedData.name,
          description: cleanedData.description,
          inputSchema: cleanedData.inputSchema,
          outputSchema: cleanedData.outputSchema,
          implementation: cleanedData.implementation,
          isActive: cleanedData.isActive,
        },
        select: {
          id: true,
          entityId: true,
          name: true,
          description: true,
          inputSchema: true,
          outputSchema: true,
          implementation: true,
          isActive: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'inputSchema',
        message: 'inputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'outputSchema',
        message: 'outputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'implementation',
        message: 'implementation',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ToolDefinitionPatch;
    const client = getClient();
    const result = await client.toolDefinition
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          name: cleanedData.name,
          description: cleanedData.description,
          inputSchema: cleanedData.inputSchema,
          outputSchema: cleanedData.outputSchema,
          implementation: cleanedData.implementation,
          isActive: cleanedData.isActive,
        },
        select: {
          id: true,
          entityId: true,
          name: true,
          description: true,
          inputSchema: true,
          outputSchema: true,
          implementation: true,
          isActive: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.toolDefinition
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
