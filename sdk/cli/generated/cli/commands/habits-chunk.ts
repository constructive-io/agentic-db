/**
 * CLI commands for HabitsChunk
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateHabitsChunkInput, HabitsChunkPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  habitsId: 'uuid',
  content: 'string',
  chunkIndex: 'int',
  embedding: 'string',
  metadata: 'json',
  createdAt: 'string',
  updatedAt: 'string',
  embeddingVectorDistance: 'float',
  searchScore: 'float',
};
const usage =
  '\nhabits-chunk <command>\n\nCommands:\n  list                  List all habitsChunk records\n  get                   Get a habitsChunk by ID\n  create                Create a new habitsChunk\n  update                Update an existing habitsChunk\n  delete                Delete a habitsChunk\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.habitsChunk
      .findMany({
        select: {
          id: true,
          habitsId: true,
          content: true,
          chunkIndex: true,
          embedding: true,
          metadata: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.habitsChunk
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          habitsId: true,
          content: true,
          chunkIndex: true,
          embedding: true,
          metadata: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'habitsId',
        message: 'habitsId',
        required: true,
      },
      {
        type: 'text',
        name: 'content',
        message: 'content',
        required: true,
      },
      {
        type: 'text',
        name: 'chunkIndex',
        message: 'chunkIndex',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateHabitsChunkInput['habitsChunk'];
    const client = getClient();
    const result = await client.habitsChunk
      .create({
        data: {
          habitsId: cleanedData.habitsId,
          content: cleanedData.content,
          chunkIndex: cleanedData.chunkIndex,
          embedding: cleanedData.embedding,
          metadata: cleanedData.metadata,
        },
        select: {
          id: true,
          habitsId: true,
          content: true,
          chunkIndex: true,
          embedding: true,
          metadata: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'habitsId',
        message: 'habitsId',
        required: false,
      },
      {
        type: 'text',
        name: 'content',
        message: 'content',
        required: false,
      },
      {
        type: 'text',
        name: 'chunkIndex',
        message: 'chunkIndex',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'metadata',
        message: 'metadata',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as HabitsChunkPatch;
    const client = getClient();
    const result = await client.habitsChunk
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          habitsId: cleanedData.habitsId,
          content: cleanedData.content,
          chunkIndex: cleanedData.chunkIndex,
          embedding: cleanedData.embedding,
          metadata: cleanedData.metadata,
        },
        select: {
          id: true,
          habitsId: true,
          content: true,
          chunkIndex: true,
          embedding: true,
          metadata: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.habitsChunk
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
