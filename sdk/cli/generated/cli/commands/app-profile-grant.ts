/**
 * CLI commands for AppProfileGrant
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateAppProfileGrantInput, AppProfileGrantPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  membershipId: 'uuid',
  profileId: 'uuid',
  grantorId: 'uuid',
  isGrant: 'boolean',
  createdAt: 'string',
  updatedAt: 'string',
};
const usage =
  '\napp-profile-grant <command>\n\nCommands:\n  list                  List all appProfileGrant records\n  get                   Get a appProfileGrant by ID\n  create                Create a new appProfileGrant\n  update                Update an existing appProfileGrant\n  delete                Delete a appProfileGrant\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.appProfileGrant
      .findMany({
        select: {
          id: true,
          membershipId: true,
          profileId: true,
          grantorId: true,
          isGrant: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.appProfileGrant
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          membershipId: true,
          profileId: true,
          grantorId: true,
          isGrant: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'membershipId',
        message: 'membershipId',
        required: true,
      },
      {
        type: 'text',
        name: 'profileId',
        message: 'profileId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'grantorId',
        message: 'grantorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isGrant',
        message: 'isGrant',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateAppProfileGrantInput['appProfileGrant'];
    const client = getClient();
    const result = await client.appProfileGrant
      .create({
        data: {
          membershipId: cleanedData.membershipId,
          profileId: cleanedData.profileId,
          grantorId: cleanedData.grantorId,
          isGrant: cleanedData.isGrant,
        },
        select: {
          id: true,
          membershipId: true,
          profileId: true,
          grantorId: true,
          isGrant: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'membershipId',
        message: 'membershipId',
        required: false,
      },
      {
        type: 'text',
        name: 'profileId',
        message: 'profileId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'grantorId',
        message: 'grantorId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isGrant',
        message: 'isGrant',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as AppProfileGrantPatch;
    const client = getClient();
    const result = await client.appProfileGrant
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          membershipId: cleanedData.membershipId,
          profileId: cleanedData.profileId,
          grantorId: cleanedData.grantorId,
          isGrant: cleanedData.isGrant,
        },
        select: {
          id: true,
          membershipId: true,
          profileId: true,
          grantorId: true,
          isGrant: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.appProfileGrant
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
