/**
 * CLI commands for CalendarEvent
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateCalendarEventInput, CalendarEventPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  providerEventId: 'string',
  title: 'string',
  description: 'string',
  startTime: 'string',
  endTime: 'string',
  meetingUrl: 'string',
  organizerContactId: 'uuid',
  tags: 'string',
  createdAt: 'string',
  updatedAt: 'string',
  embeddingText: 'string',
  searchTsv: 'string',
  embedding: 'string',
  embeddingStale: 'boolean',
  calendarId: 'uuid',
  searchTsvRank: 'float',
  embeddingTextBm25Score: 'float',
  embeddingVectorDistance: 'float',
  providerEventIdTrgmSimilarity: 'float',
  titleTrgmSimilarity: 'float',
  descriptionTrgmSimilarity: 'float',
  meetingUrlTrgmSimilarity: 'float',
  embeddingTextTrgmSimilarity: 'float',
  searchScore: 'float',
};
const usage =
  '\ncalendar-event <command>\n\nCommands:\n  list                  List all calendarEvent records\n  get                   Get a calendarEvent by ID\n  create                Create a new calendarEvent\n  update                Update an existing calendarEvent\n  delete                Delete a calendarEvent\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.calendarEvent
      .findMany({
        select: {
          id: true,
          entityId: true,
          providerEventId: true,
          title: true,
          description: true,
          startTime: true,
          endTime: true,
          meetingUrl: true,
          organizerContactId: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          calendarId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.calendarEvent
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          providerEventId: true,
          title: true,
          description: true,
          startTime: true,
          endTime: true,
          meetingUrl: true,
          organizerContactId: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          calendarId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'providerEventId',
        message: 'providerEventId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'title',
        message: 'title',
        required: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startTime',
        message: 'startTime',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'endTime',
        message: 'endTime',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'meetingUrl',
        message: 'meetingUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'organizerContactId',
        message: 'organizerContactId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'embeddingStale',
        message: 'embeddingStale',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'calendarId',
        message: 'calendarId',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateCalendarEventInput['calendarEvent'];
    const client = getClient();
    const result = await client.calendarEvent
      .create({
        data: {
          entityId: cleanedData.entityId,
          providerEventId: cleanedData.providerEventId,
          title: cleanedData.title,
          description: cleanedData.description,
          startTime: cleanedData.startTime,
          endTime: cleanedData.endTime,
          meetingUrl: cleanedData.meetingUrl,
          organizerContactId: cleanedData.organizerContactId,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
          embeddingStale: cleanedData.embeddingStale,
          calendarId: cleanedData.calendarId,
        },
        select: {
          id: true,
          entityId: true,
          providerEventId: true,
          title: true,
          description: true,
          startTime: true,
          endTime: true,
          meetingUrl: true,
          organizerContactId: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          calendarId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'providerEventId',
        message: 'providerEventId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'title',
        message: 'title',
        required: false,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'startTime',
        message: 'startTime',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'endTime',
        message: 'endTime',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'meetingUrl',
        message: 'meetingUrl',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'organizerContactId',
        message: 'organizerContactId',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'embeddingStale',
        message: 'embeddingStale',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'calendarId',
        message: 'calendarId',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CalendarEventPatch;
    const client = getClient();
    const result = await client.calendarEvent
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          providerEventId: cleanedData.providerEventId,
          title: cleanedData.title,
          description: cleanedData.description,
          startTime: cleanedData.startTime,
          endTime: cleanedData.endTime,
          meetingUrl: cleanedData.meetingUrl,
          organizerContactId: cleanedData.organizerContactId,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
          embeddingStale: cleanedData.embeddingStale,
          calendarId: cleanedData.calendarId,
        },
        select: {
          id: true,
          entityId: true,
          providerEventId: true,
          title: true,
          description: true,
          startTime: true,
          endTime: true,
          meetingUrl: true,
          organizerContactId: true,
          tags: true,
          createdAt: true,
          updatedAt: true,
          embeddingText: true,
          embedding: true,
          embeddingStale: true,
          calendarId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.calendarEvent
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
