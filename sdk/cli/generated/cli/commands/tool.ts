/**
 * CLI commands for Tool
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateToolInput, ToolPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  createdAt: 'string',
  updatedAt: 'string',
  name: 'string',
  description: 'string',
  type: 'string',
  inputSchema: 'json',
  outputSchema: 'json',
  endpoint: 'string',
  authMethod: 'string',
  isActive: 'boolean',
  tags: 'string',
  embeddingText: 'string',
  embedding: 'string',
  embeddingDistance: 'float',
};
const usage =
  '\ntool <command>\n\nCommands:\n  list                  List all tool records\n  get                   Get a tool by ID\n  create                Create a new tool\n  update                Update an existing tool\n  delete                Delete a tool\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.tool
      .findMany({
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          type: true,
          inputSchema: true,
          outputSchema: true,
          endpoint: true,
          authMethod: true,
          isActive: true,
          tags: true,
          embeddingText: true,
          embedding: true,
          embeddingDistance: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.tool
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          type: true,
          inputSchema: true,
          outputSchema: true,
          endpoint: true,
          authMethod: true,
          isActive: true,
          tags: true,
          embeddingText: true,
          embedding: true,
          embeddingDistance: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: true,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'type',
        message: 'type',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'inputSchema',
        message: 'inputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'outputSchema',
        message: 'outputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'endpoint',
        message: 'endpoint',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authMethod',
        message: 'authMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as CreateToolInput['tool'];
    const client = getClient();
    const result = await client.tool
      .create({
        data: {
          entityId: cleanedData.entityId,
          name: cleanedData.name,
          description: cleanedData.description,
          type: cleanedData.type,
          inputSchema: cleanedData.inputSchema,
          outputSchema: cleanedData.outputSchema,
          endpoint: cleanedData.endpoint,
          authMethod: cleanedData.authMethod,
          isActive: cleanedData.isActive,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          type: true,
          inputSchema: true,
          outputSchema: true,
          endpoint: true,
          authMethod: true,
          isActive: true,
          tags: true,
          embeddingText: true,
          embedding: true,
          embeddingDistance: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'name',
        message: 'name',
        required: false,
      },
      {
        type: 'text',
        name: 'description',
        message: 'description',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'type',
        message: 'type',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'inputSchema',
        message: 'inputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'outputSchema',
        message: 'outputSchema',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'endpoint',
        message: 'endpoint',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'authMethod',
        message: 'authMethod',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isActive',
        message: 'isActive',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'tags',
        message: 'tags',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embeddingText',
        message: 'embeddingText',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'embedding',
        message: 'embedding',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ToolPatch;
    const client = getClient();
    const result = await client.tool
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          name: cleanedData.name,
          description: cleanedData.description,
          type: cleanedData.type,
          inputSchema: cleanedData.inputSchema,
          outputSchema: cleanedData.outputSchema,
          endpoint: cleanedData.endpoint,
          authMethod: cleanedData.authMethod,
          isActive: cleanedData.isActive,
          tags: cleanedData.tags,
          embeddingText: cleanedData.embeddingText,
          embedding: cleanedData.embedding,
        },
        select: {
          id: true,
          entityId: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          description: true,
          type: true,
          inputSchema: true,
          outputSchema: true,
          endpoint: true,
          authMethod: true,
          isActive: true,
          tags: true,
          embeddingText: true,
          embedding: true,
          embeddingDistance: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.tool
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
