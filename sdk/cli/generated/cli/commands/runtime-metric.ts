/**
 * CLI commands for RuntimeMetric
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, parseFindFirstArgs, parseFindManyArgs, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type {
  CreateRuntimeMetricInput,
  RuntimeMetricPatch,
  RuntimeMetricSelect,
  RuntimeMetricFilter,
  RuntimeMetricOrderBy,
} from '../../orm/input-types';
import type { FindManyArgs, FindFirstArgs } from '../../orm/select-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  runtimeStateId: 'uuid',
  metricName: 'string',
  metricValue: 'string',
  unit: 'string',
  meta: 'json',
  createdAt: 'string',
  updatedAt: 'string',
};
const usage =
  '\nruntime-metric <command>\n\nCommands:\n  list                  List runtimeMetric records\n  find-first            Find first matching runtimeMetric record\n  get                   Get a runtimeMetric by ID\n  create                Create a new runtimeMetric\n  update                Update an existing runtimeMetric\n  delete                Delete a runtimeMetric\n\nList Options:\n  --limit <n>           Max number of records to return (forward pagination)\n  --last <n>            Number of records from the end (backward pagination)\n  --after <cursor>      Cursor for forward pagination\n  --before <cursor>     Cursor for backward pagination\n  --offset <n>          Number of records to skip\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.name.equalTo foo)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n  --orderBy <values>    Comma-separated ordering values (e.g. NAME_ASC,CREATED_AT_DESC)\n\nFind-First Options:\n  --select <fields>     Comma-separated list of fields to return\n  --where.<field>.<op>  Filter (dot-notation, e.g. --where.status.equalTo active)\n  --condition.<f>.<op>  Condition filter (dot-notation)\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'find-first', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'find-first':
      return handleFindFirst(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      id: true,
      entityId: true,
      runtimeStateId: true,
      metricName: true,
      metricValue: true,
      unit: true,
      meta: true,
      createdAt: true,
      updatedAt: true,
    };
    const findManyArgs = parseFindManyArgs<
      FindManyArgs<RuntimeMetricSelect, RuntimeMetricFilter, never, RuntimeMetricOrderBy> & {
        select: RuntimeMetricSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.runtimeMetric.findMany(findManyArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleFindFirst(argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const defaultSelect = {
      id: true,
      entityId: true,
      runtimeStateId: true,
      metricName: true,
      metricValue: true,
      unit: true,
      meta: true,
      createdAt: true,
      updatedAt: true,
    };
    const findFirstArgs = parseFindFirstArgs<
      FindFirstArgs<RuntimeMetricSelect, RuntimeMetricFilter, never> & {
        select: RuntimeMetricSelect;
      }
    >(argv, defaultSelect);
    const client = getClient();
    const result = await client.runtimeMetric.findFirst(findFirstArgs).execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to find record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.runtimeMetric
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          runtimeStateId: true,
          metricName: true,
          metricValue: true,
          unit: true,
          meta: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'runtimeStateId',
        message: 'runtimeStateId',
        required: true,
      },
      {
        type: 'text',
        name: 'metricName',
        message: 'metricName',
        required: true,
      },
      {
        type: 'text',
        name: 'metricValue',
        message: 'metricValue',
        required: true,
      },
      {
        type: 'text',
        name: 'unit',
        message: 'unit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'meta',
        message: 'meta',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateRuntimeMetricInput['runtimeMetric'];
    const client = getClient();
    const result = await client.runtimeMetric
      .create({
        data: {
          entityId: cleanedData.entityId,
          runtimeStateId: cleanedData.runtimeStateId,
          metricName: cleanedData.metricName,
          metricValue: cleanedData.metricValue,
          unit: cleanedData.unit,
          meta: cleanedData.meta,
        },
        select: {
          id: true,
          entityId: true,
          runtimeStateId: true,
          metricName: true,
          metricValue: true,
          unit: true,
          meta: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'runtimeStateId',
        message: 'runtimeStateId',
        required: false,
      },
      {
        type: 'text',
        name: 'metricName',
        message: 'metricName',
        required: false,
      },
      {
        type: 'text',
        name: 'metricValue',
        message: 'metricValue',
        required: false,
      },
      {
        type: 'text',
        name: 'unit',
        message: 'unit',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'json',
        name: 'meta',
        message: 'meta',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as RuntimeMetricPatch;
    const client = getClient();
    const result = await client.runtimeMetric
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          runtimeStateId: cleanedData.runtimeStateId,
          metricName: cleanedData.metricName,
          metricValue: cleanedData.metricValue,
          unit: cleanedData.unit,
          meta: cleanedData.meta,
        },
        select: {
          id: true,
          entityId: true,
          runtimeStateId: true,
          metricName: true,
          metricValue: true,
          unit: true,
          meta: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.runtimeMetric
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
