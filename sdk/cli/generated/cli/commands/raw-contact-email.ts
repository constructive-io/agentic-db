/**
 * CLI commands for RawContactEmail
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateRawContactEmailInput, RawContactEmailPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  rawContactId: 'uuid',
  email: 'string',
  emailType: 'string',
  isPrimary: 'boolean',
  source: 'string',
  confidence: 'string',
  ingestedAt: 'string',
  createdAt: 'string',
  updatedAt: 'string',
};
const usage =
  '\nraw-contact-email <command>\n\nCommands:\n  list                  List all rawContactEmail records\n  get                   Get a rawContactEmail by ID\n  create                Create a new rawContactEmail\n  update                Update an existing rawContactEmail\n  delete                Delete a rawContactEmail\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.rawContactEmail
      .findMany({
        select: {
          id: true,
          entityId: true,
          rawContactId: true,
          email: true,
          emailType: true,
          isPrimary: true,
          source: true,
          confidence: true,
          ingestedAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.rawContactEmail
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          rawContactId: true,
          email: true,
          emailType: true,
          isPrimary: true,
          source: true,
          confidence: true,
          ingestedAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'rawContactId',
        message: 'rawContactId',
        required: true,
      },
      {
        type: 'text',
        name: 'email',
        message: 'email',
        required: true,
      },
      {
        type: 'text',
        name: 'emailType',
        message: 'emailType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPrimary',
        message: 'isPrimary',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'source',
        message: 'source',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'confidence',
        message: 'confidence',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'ingestedAt',
        message: 'ingestedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateRawContactEmailInput['rawContactEmail'];
    const client = getClient();
    const result = await client.rawContactEmail
      .create({
        data: {
          entityId: cleanedData.entityId,
          rawContactId: cleanedData.rawContactId,
          email: cleanedData.email,
          emailType: cleanedData.emailType,
          isPrimary: cleanedData.isPrimary,
          source: cleanedData.source,
          confidence: cleanedData.confidence,
          ingestedAt: cleanedData.ingestedAt,
        },
        select: {
          id: true,
          entityId: true,
          rawContactId: true,
          email: true,
          emailType: true,
          isPrimary: true,
          source: true,
          confidence: true,
          ingestedAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'rawContactId',
        message: 'rawContactId',
        required: false,
      },
      {
        type: 'text',
        name: 'email',
        message: 'email',
        required: false,
      },
      {
        type: 'text',
        name: 'emailType',
        message: 'emailType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPrimary',
        message: 'isPrimary',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'source',
        message: 'source',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'confidence',
        message: 'confidence',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'ingestedAt',
        message: 'ingestedAt',
        required: false,
        skipPrompt: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as RawContactEmailPatch;
    const client = getClient();
    const result = await client.rawContactEmail
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          rawContactId: cleanedData.rawContactId,
          email: cleanedData.email,
          emailType: cleanedData.emailType,
          isPrimary: cleanedData.isPrimary,
          source: cleanedData.source,
          confidence: cleanedData.confidence,
          ingestedAt: cleanedData.ingestedAt,
        },
        select: {
          id: true,
          entityId: true,
          rawContactId: true,
          email: true,
          emailType: true,
          isPrimary: true,
          source: true,
          confidence: true,
          ingestedAt: true,
          createdAt: true,
          updatedAt: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.rawContactEmail
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
