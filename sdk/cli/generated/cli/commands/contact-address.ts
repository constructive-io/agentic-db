/**
 * CLI commands for ContactAddress
 * @generated by @constructive-io/graphql-codegen
 * DO NOT EDIT - changes will be overwritten
 */
import { CLIOptions, Inquirerer, extractFirst } from 'inquirerer';
import { getClient } from '../executor';
import { coerceAnswers, stripUndefined } from '../utils';
import type { FieldSchema } from '../utils';
import type { CreateContactAddressInput, ContactAddressPatch } from '../../orm/input-types';
const fieldSchema: FieldSchema = {
  id: 'uuid',
  entityId: 'uuid',
  street: 'string',
  city: 'string',
  state: 'string',
  postalCode: 'string',
  country: 'string',
  addressType: 'string',
  isPrimary: 'boolean',
  createdAt: 'string',
  updatedAt: 'string',
  contactId: 'uuid',
};
const usage =
  '\ncontact-address <command>\n\nCommands:\n  list                  List all contactAddress records\n  get                   Get a contactAddress by ID\n  create                Create a new contactAddress\n  update                Update an existing contactAddress\n  delete                Delete a contactAddress\n\n  --help, -h            Show this help message\n';
export default async (
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer,
  _options: CLIOptions
) => {
  if (argv.help || argv.h) {
    console.log(usage);
    process.exit(0);
  }
  const { first: subcommand, newArgv } = extractFirst(argv);
  if (!subcommand) {
    const answer = await prompter.prompt(argv, [
      {
        type: 'autocomplete',
        name: 'subcommand',
        message: 'What do you want to do?',
        options: ['list', 'get', 'create', 'update', 'delete'],
      },
    ]);
    return handleTableSubcommand(answer.subcommand as string, newArgv, prompter);
  }
  return handleTableSubcommand(subcommand, newArgv, prompter);
};
async function handleTableSubcommand(
  subcommand: string,
  argv: Partial<Record<string, unknown>>,
  prompter: Inquirerer
) {
  switch (subcommand) {
    case 'list':
      return handleList(argv, prompter);
    case 'get':
      return handleGet(argv, prompter);
    case 'create':
      return handleCreate(argv, prompter);
    case 'update':
      return handleUpdate(argv, prompter);
    case 'delete':
      return handleDelete(argv, prompter);
    default:
      console.log(usage);
      process.exit(1);
  }
}
async function handleList(_argv: Partial<Record<string, unknown>>, _prompter: Inquirerer) {
  try {
    const client = getClient();
    const result = await client.contactAddress
      .findMany({
        select: {
          id: true,
          entityId: true,
          street: true,
          city: true,
          state: true,
          postalCode: true,
          country: true,
          addressType: true,
          isPrimary: true,
          createdAt: true,
          updatedAt: true,
          contactId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to list records.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleGet(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const answers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const client = getClient();
    const result = await client.contactAddress
      .findOne({
        id: answers.id as string,
        select: {
          id: true,
          entityId: true,
          street: true,
          city: true,
          state: true,
          postalCode: true,
          country: true,
          addressType: true,
          isPrimary: true,
          createdAt: true,
          updatedAt: true,
          contactId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Record not found.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleCreate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: true,
      },
      {
        type: 'text',
        name: 'street',
        message: 'street',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'city',
        message: 'city',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'state',
        message: 'state',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'postalCode',
        message: 'postalCode',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'country',
        message: 'country',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'addressType',
        message: 'addressType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPrimary',
        message: 'isPrimary',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'contactId',
        message: 'contactId',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(
      answers,
      fieldSchema
    ) as CreateContactAddressInput['contactAddress'];
    const client = getClient();
    const result = await client.contactAddress
      .create({
        data: {
          entityId: cleanedData.entityId,
          street: cleanedData.street,
          city: cleanedData.city,
          state: cleanedData.state,
          postalCode: cleanedData.postalCode,
          country: cleanedData.country,
          addressType: cleanedData.addressType,
          isPrimary: cleanedData.isPrimary,
          contactId: cleanedData.contactId,
        },
        select: {
          id: true,
          entityId: true,
          street: true,
          city: true,
          state: true,
          postalCode: true,
          country: true,
          addressType: true,
          isPrimary: true,
          createdAt: true,
          updatedAt: true,
          contactId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to create record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleUpdate(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
      {
        type: 'text',
        name: 'entityId',
        message: 'entityId',
        required: false,
      },
      {
        type: 'text',
        name: 'street',
        message: 'street',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'city',
        message: 'city',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'state',
        message: 'state',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'postalCode',
        message: 'postalCode',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'country',
        message: 'country',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'addressType',
        message: 'addressType',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'boolean',
        name: 'isPrimary',
        message: 'isPrimary',
        required: false,
        skipPrompt: true,
      },
      {
        type: 'text',
        name: 'contactId',
        message: 'contactId',
        required: false,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const cleanedData = stripUndefined(answers, fieldSchema) as ContactAddressPatch;
    const client = getClient();
    const result = await client.contactAddress
      .update({
        where: {
          id: answers.id as string,
        },
        data: {
          entityId: cleanedData.entityId,
          street: cleanedData.street,
          city: cleanedData.city,
          state: cleanedData.state,
          postalCode: cleanedData.postalCode,
          country: cleanedData.country,
          addressType: cleanedData.addressType,
          isPrimary: cleanedData.isPrimary,
          contactId: cleanedData.contactId,
        },
        select: {
          id: true,
          entityId: true,
          street: true,
          city: true,
          state: true,
          postalCode: true,
          country: true,
          addressType: true,
          isPrimary: true,
          createdAt: true,
          updatedAt: true,
          contactId: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to update record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
async function handleDelete(argv: Partial<Record<string, unknown>>, prompter: Inquirerer) {
  try {
    const rawAnswers = await prompter.prompt(argv, [
      {
        type: 'text',
        name: 'id',
        message: 'id',
        required: true,
      },
    ]);
    const answers = coerceAnswers(rawAnswers, fieldSchema);
    const client = getClient();
    const result = await client.contactAddress
      .delete({
        where: {
          id: answers.id as string,
        },
        select: {
          id: true,
        },
      })
      .execute();
    console.log(JSON.stringify(result, null, 2));
  } catch (error) {
    console.error('Failed to delete record.');
    if (error instanceof Error) {
      console.error(error.message);
    }
    process.exit(1);
  }
}
